import { ReactNode } from 'react';

export interface FormGroupProps {
  title?: string;
  subtitle?: string;
  children?: ReactNode;
}

export interface FormModalGroupProps {
  children?: ReactNode;
}
